import { colors } from './colors';

export const theme = {
  colors,
  extend: {
    fontFamily: {
      sans: ['Borna', 'sans-serif'],
    },
    lineHeight: {
      '11': '2.75rem',
    },
  },
};

export default theme;
